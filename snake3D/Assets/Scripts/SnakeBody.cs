using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SnakeBody : MonoBehaviour
{
    //instalize null - playerLastmove is where player is
    //gets last player movement - GetPlayerPosition.cs
    //this.goes to that last point..
    //repeats of each gameobject.

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
